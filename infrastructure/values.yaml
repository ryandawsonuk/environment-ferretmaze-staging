keycloak:
  enabled: true
  keycloak:
    username: admin
    password: admin
    persistence:
      deployPostgres: false
      dbVendor: H2
    ingress:
      enabled: true
      path: /
      hosts:
        - "jx-staging-quickstart-http.jx-staging.activiti.envalfresco.com"
      annotations:
    livenessProbe:
      initialDelaySeconds: 140
    extraVolumes: |
      - name: realm-secret
        secret:
          secretName: {{ .Release.Name }}-realm-secret
    extraVolumeMounts: |
      - name: realm-secret
        mountPath: "/realm/"
        readOnly: true
    extraArgs: -Dkeycloak.import=activiti-realm.json
    preStartScript: |
      /opt/jboss/keycloak/bin/add-user.sh -u admin -p admin
      /opt/jboss/keycloak/bin/add-user-keycloak.sh -r master -u admin -p admin
      cp /realm/activiti-realm.json .
      sed -i 's/localhost:3000/jx-staging-quickstart-http.jx-staging.activiti.envalfresco.com/g' activiti-realm.json
      sed -i 's/"http:\/\/localhost:\*"/"http://jx-staging-quickstart-http.jx-staging.activiti.envalfresco.com:*","http://jx-staging-quickstart-http.jx-staging.activiti.envalfresco.com","http://localhost:*"/g' activiti-realm.json